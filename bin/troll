#!/usr/bin/env lua
-- Troll: A bullshit-free test runner for Lua

run_tests, print_results, test, context, it, before = require 'troll.core'()

require 'test.test_helper'

for index = 1, #arg do
  dofile(arg[index])
end

run_tests()
print_results()
